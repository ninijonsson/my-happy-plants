package se.mau.myhappyplants.library;

/**
 * Entity connecting User + PlantCatalog + tag(?)
 * JPA entity representing a plant owned by a user ("My Plants").
 * Links a User to a PlantCatalog entry and stores user-specific metadata (nickname, watering, tags).
 */
public class UserPlant {
}
